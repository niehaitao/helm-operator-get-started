---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: podinfo-dev
  namespace: dev
  annotations:
    fluxcd.io/automated: "true"
    filter.fluxcd.io/chart-image: glob:dev-*
spec:
  releaseName: podinfo-dev
  chart:
    git: https://github.com/niehaitao/helm-operator-get-started.git
    path: charts/podinfo
    ref: master
  values:
    image:
      repository: haitaonie/podinfo
      tag: dev-hdtwcel9
    replicaCount: 1
    hpa:
      enabled: false
